// Empty string responses, because don't need any actual data to be returned
// upon request completion for these functionalities.
export type DeleteUserResponse = string;
export type LogoutUserResponse = string;
export type AdminUpdateUserRoleResponse = string;
