import React from "react";

// TODO: anyone can be an admin of a community (regular users too)
const TransferCommunityForm: React.FC = () => {
  return <></>;
};

export default TransferCommunityForm;
