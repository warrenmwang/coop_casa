import React from "react";

// TODO: allow a lister to transfer their property to another lister user profile
// therefore all listers will be able to automatically opted into the coop network of listers
const TransferPropertyForm: React.FC = () => {
  return <></>;
};

export default TransferPropertyForm;
